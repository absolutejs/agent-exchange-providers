{
  "name": "@absolutejs/agent-exchange-providers",
  "private": true,
  "type": "module",
  "workspaces": [
    "broker",
    "conformance",
    "google",
    "microsoft",
    "oauth",
    "oauth-stores",
    "oauth-webcrypto",
    "webauthn"
  ],
  "scripts": {
    "build": "bun run --cwd conformance build && bun run --cwd oauth build && bun run --cwd broker build && bun run --cwd webauthn build && bun run --cwd oauth-webcrypto build && bun run --cwd oauth-stores build && bun run --cwd google build && bun run --cwd microsoft build",
    "check:package": "bun run format:check && bun run typecheck && bun run test && bun run build",
    "format": "prettier --write \"./**/*.{ts,json,md}\"",
    "format:check": "prettier --check \"./**/*.{ts,json,md}\"",
    "test": "bun run --filter './*' test",
    "typecheck": "bun run --filter './*' typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "prettier": "^3.9.0",
    "typescript": "^5.9.0"
  }
}
