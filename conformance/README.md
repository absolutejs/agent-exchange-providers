# @absolutejs/agent-exchange-provider-conformance

Shared capability checks and cryptographic DPoP verification for Agent Exchange
providers. Provider adapters publish facts; this package decides whether those
facts meet the phishing-resistant OAuth profile.

Unknown or unavailable features are failures, not optimistic defaults.
