{
  "name": "@absolutejs/browser-session",
  "version": "0.2.1",
  "type": "module",
  "description": "Owner-bound expiring browser sessions for human handoff and protected agent destinations.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/absolutejs/agent-exchange-providers.git",
    "directory": "browser-session"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./playwright": {
      "types": "./dist/playwright.d.ts",
      "import": "./dist/playwright.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test tests/",
    "build": "bun build src/index.ts --outdir dist --target=browser && bun build src/playwright.ts --outdir dist --target=bun --external playwright-core --external @absolutejs/egress/transport && tsc -p tsconfig.build.json",
    "check:package": "bun run typecheck && bun run test && bun run build",
    "prepublishOnly": "bun run check:package"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "@absolutejs/egress": "0.3.1",
    "playwright-core": "1.63.0"
  }
}
