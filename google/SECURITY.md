# Security

`gmail.readonly` is a Google restricted scope and can trigger verification and
security-assessment requirements. Request only the mailbox permissions actually
needed and keep client secrets, refresh tokens, and bearer access tokens in a
trusted BFF or managed service.
